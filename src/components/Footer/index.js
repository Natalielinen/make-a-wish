import React from 'react';
import styles from './styles.module.scss';

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>Here you can make your wish, no matter how ridiculous it is.</p>
        </footer>
    );
};

export default Footer;
